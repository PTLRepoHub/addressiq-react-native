# Dependencies
node_modules/

# Local credentials — copy credentials.template.json → credentials.json
src/config/credentials.json

# iOS (CocoaPods + build output — regenerated by `pod install` / Xcode)
ios/Pods/
ios/build/
ios/.xcode.env.local
*.xcuserstate
*.xcworkspace/xcuserdata/

# Android (Gradle build output)
android/.gradle/
android/.cxx/
android/build/
android/app/build/
android/local.properties

# Ruby (Bundler — regenerated by `bundle install`)
vendor/bundle/
.bundle/

# Metro / RN
.metro-health-check*
*.jsbundle

# Misc
*.log
.DS_Store
